#include <stdio.h>
/**
 * main - numbers
 *
 * Return: 0 - numbers
 *
 */
int main(void)
{
	int x;

	do {
		for (x = 0; x <= 9; x++)
			putchar(x + 48);
	} while (x < 9);
	putchar('\n');
return (0);
}
