#include <stdio.h>
/**
 * main - alphabet with uppercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int z;

	do {
		for (z = 'a'; z <= 'z'; z++)
			putchar(c);
	} while (z < 'z');
	do {
		for (z = 'A'; z <= 'Z'; z++)
			putchar(z);
	} while (z < 'Z');
	putchar('\n');
	return (0);
}
